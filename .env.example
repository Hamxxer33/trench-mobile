# Placeholder only — copy to .env.local (gitignored). Never commit real secrets.
# EXPO_PUBLIC_PRIVY_APP_ID=
# EXPO_PUBLIC_CDP_API_KEY=
# EAS / Play credentials live in EAS dashboard (`eas credentials`), not in git.
